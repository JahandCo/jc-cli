{
  "name": "test-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "webpack serve --config webpack.client.js --mode development",
    "build": "webpack --config webpack.client.js --mode production && webpack --config webpack.rules.js && node -e \"require('fs').mkdirSync('dist',{recursive:true});require('fs').writeFileSync('dist/client.js','// placeholder -- jc deploy requires this exact zip entry, but nothing loads it at runtime; the real client bundle is out/client.js, zipped in full alongside out/index.html. See jc-cli/cmd/deploy.go createBundleArchive.\\n')\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@jahandco/game-sdk": "^1.0.0",
    "@jahandco/interactive-protocol": "^0.4.0",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "webpack": "^5.95.0",
    "webpack-cli": "^5.1.4",
    "webpack-dev-server": "^5.1.0",
    "ts-loader": "^9.5.1",
    "copy-webpack-plugin": "^12.0.2",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
