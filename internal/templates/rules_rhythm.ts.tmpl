import type { BridgeEnvelope } from "@jahandco/interactive-protocol";

// Rhythm titles run on the Midnight Circuit (MC)-derived architecture, which
// hasn't been extracted/rebuilt for this SDK yet -- this is a placeholder,
// not a real starting point. Rhythm tolerates zero non-rhythm features: the
// moment you add anything outside this architecture, the title becomes a
// Remix classified by whatever else it mixes in, not "Rhythm" anymore.
// user.getProfile is handled by play-api now, not a rules-bundle stub.

export function handleIntent(envelope: BridgeEnvelope): BridgeEnvelope | undefined {
  return undefined;
}
