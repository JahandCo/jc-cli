{
  "name": "{{.Slug}}",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build && esbuild src/rules.ts --bundle --outfile=dist/rules.js --format=esm --platform=node --target=es2022 && echo \"// unused -- see out/index.html (produced by next build) for the real client entry. developer-api's deploy bundle validation still requires this exact file to exist -- see jc deploy's createBundleArchive.\" > dist/client.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "next": "{{.NextVersion}}",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "@jahandco/interactive-sdk": "{{.SdkVersion}}",
    "@jahandco/interactive-protocol": "{{.ProtocolVersion}}",
    "@jahandco/interactive-mechanics": "{{.MechanicsVersion}}",
    "phaser": "{{.PhaserVersion}}"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.5",
    "@types/react-dom": "^19.0.2"
  }
}
