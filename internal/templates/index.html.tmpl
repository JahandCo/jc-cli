<!doctype html>
<html>
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>{{.DisplayName}}</title>
<style>
  body { margin: 0; background: #0b0f19; display: flex; align-items: center; justify-content: center; min-height: 100vh; }
</style>
</head>
<body>
  <!-- Phaser's Game config (parent: "game", see client.ts) injects its own
       canvas here -- no DOM elements to manage by hand. -->
  <div id="game"></div>
  <script type="module" src="./dist/client.js"></script>
</body>
</html>
